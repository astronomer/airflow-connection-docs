name: Validate connections.json
on: [push]
jobs:
  validate-generated-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          cache: 'pip'

      - run: pip install -r scripts/requirements.txt

      - name: Generate connections.json from dev
        run: |
          python3 scripts/generate.py dev

      - name: Print connections.json
        run: |
          cat connections-dev.json

      - name: Dry-Run connections-dev.json
        run: |
          curl -X POST \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.DEV_BEARER_TOKEN }}" \
              -d @connections-dev.json \
              https://api.astronomer-dev.io/registryV2/v1alpha1/organizations/astronomer/connection-types?isDryRun=true